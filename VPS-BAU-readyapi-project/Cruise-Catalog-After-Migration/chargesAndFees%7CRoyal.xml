<?xml version="1.0" encoding="UTF-8"?>
<con:testCase xmlns:con="http://eviware.com/soapui/config" id="e20aa3a2-2275-441c-993a-ff5009a38896" discardOkResults="false" failOnError="false" failTestCaseOnErrors="true" keepSession="false" name="chargesAndFees|Royal" searchProperties="true" timeout="0">
  <con:settings id="689db48b-85fc-47c6-a346-7c8259f01fc3">
    <con:setting id="e20aa3a2-2275-441c-993a-ff5009a38896fileName">chargesAndFees%7CRoyal</con:setting>
  </con:settings>
  <con:testStep xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" type="groovy" name="loadTestData-royal" id="650faa42-a2c9-494e-82a5-83197822b0c1">
    <con:settings/>
    <con:config>
      <script>//load/fetch test data from local source
import groovy.json.JsonSlurper
import groovy.json.JsonOutput

//get user's device OS and set directory
String osType = System.properties['os.name'][0..6];
def directory = "";

if (osType == "Windows"){
	directory = "C:/VPS_JSON/vpsTestData/"
	}
else{
	directory = "/Applications/VPS_JSON/vpsTestData/" //directory for mac
	}

	
def testData = context.expand( '${#Project#testData-royal}' ) //modify this based on test case
def filePath = directory + testData
def restResponse = new JsonSlurper().parseText(new File(filePath).text)
def prettyJson = new groovy.json.JsonBuilder(restResponse).toPrettyString()</script>
    </con:config>
  </con:testStep>
  <con:testStep xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" type="groovy" name="loadTestData-market" id="34739fb4-14ae-4982-8f01-d0c91cc6d7aa">
    <con:settings/>
    <con:config>
      <script>//load/fetch test data from local source
import groovy.json.JsonSlurper
import groovy.json.JsonOutput

//get user's device OS and set directory
String osType = System.properties['os.name'][0..6];
def directory = "";

if (osType == "Windows"){
	directory = "C:/VPS_JSON/vpsTestData/"
	}
else{
	directory = "/Applications/VPS_JSON/vpsTestData/" //directory for mac
	}

	
def testData = context.expand( '${#Project#testData-market}' ) //modify this based on test case
def filePath = directory + testData
def restResponse = new JsonSlurper().parseText(new File(filePath).text)
def prettyJson = new groovy.json.JsonBuilder(restResponse).toPrettyString()</script>
    </con:config>
  </con:testStep>
  <con:testStep xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" type="groovy" name="setVariables" id="16d629b9-8941-4a78-b0eb-7cf9fb8deb5e">
    <con:settings/>
    <con:config>
      <script>//set variables from the fetched test data needed for the REST request
import groovy.json.JsonSlurper
import groovy.json.JsonOutput
import java.text.SimpleDateFormat

def shipCode = context.expand( '${loadTestData-royal#result#$[\'royal\'][\'CO\'][\'shipCode\']}' )
log.info(shipCode);
testRunner.testCase.setPropertyValue("shipCode",shipCode);

def sailDate = context.expand( '${loadTestData-royal#result#$[\'royal\'][\'CO\'][\'sailDate\']}' )
log.info(sailDate);
testRunner.testCase.setPropertyValue("sailDate",sailDate);

def currencyCode = context.expand( '${loadTestData-market#result#$[\'market\'][\'USA\'][\'currencyCode\']}' )
log.info(currencyCode);
testRunner.testCase.setPropertyValue("currencyCode",currencyCode);

def itineraryCode = context.expand( '${loadTestData-royal#result#$[\'royal\'][\'CO\'][\'itineraryCode\']}' )
log.info(itineraryCode);
testRunner.testCase.setPropertyValue("itineraryCode",itineraryCode);

def date = new Date()
def sdf = new SimpleDateFormat("YYYYMMdd")
def currentDate = sdf.format(date)
testRunner.testCase.setPropertyValue("currentDate",currentDate);</script>
    </con:config>
  </con:testStep>
  <con:testStep xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" type="restrequest" name="chargesAndFees|Royal-After" id="a2c32828-c702-4f0f-9d59-45bce23d02c9">
    <con:settings/>
    <con:config service="CruiseCatalog-Charges and Fees" resourcePath="/v1/chargesAndFees" methodName="chargesAndFees" xsi:type="con:RestRequestStep">
      <con:restRequest name="chargesAndFees|Royal-After" id="9d388b7a-eb07-4a24-b626-aa3fa4556638" mediaType="application/json" postQueryString="false">
        <con:settings>
          <con:setting id="com.eviware.soapui.impl.wsdl.WsdlRequest@request-headers">&lt;con:entry key="Content-Type" value="application/json" xmlns:con="http://eviware.com/soapui/config"/></con:setting>
        </con:settings>
        <con:request>{
	"shipCode": "${#TestCase#shipCode}",
	"itineraryCode": "${#TestCase#itineraryCode}",
	"currencyCode": "${#TestCase#currencyCode}",
	"sailDate": "${#TestCase#sailDate}",
	"bookDate": "${#TestCase#currentDate}"
}</con:request>
        <con:originalUri>http://vps.rccl.com/vps-echo/chargesAndFees/v1/chargesAndFees</con:originalUri>
        <con:assertion type="Valid HTTP Status Codes" id="2485b288-9316-4e0f-a253-5b39c9032b5d" name="Valid HTTP Status Codes">
          <con:settings/>
          <con:configuration>
            <codes>200</codes>
          </con:configuration>
        </con:assertion>
        <con:assertion type="GroovyScriptAssertion" id="b4e9e002-8708-4cfc-8914-c32ff9149e1c" name="Script Assertion">
          <con:configuration>
            <scriptText>def jsonRes = messageExchange.getResponse().contentAsString

def jsonSlurper = new groovy.json.JsonSlurper();
def parse = jsonSlurper.parseText(jsonRes)

def shipCode = parse.chargesAndFees.shipCode
log.info shipCode
assert shipCode == "NV"

def itineraryCode = parse.chargesAndFees.itineraryCode
log.info itineraryCode
assert itineraryCode == "03X023"

def currencyCode = parse.chargesAndFees.currencyCode
log.info currencyCode
assert currencyCode == "USD"</scriptText>
          </con:configuration>
        </con:assertion>
        <con:credentials>
          <con:selectedAuthProfile>No Authorization</con:selectedAuthProfile>
          <con:authType>No Authorization</con:authType>
        </con:credentials>
        <con:jmsConfig JMSDeliveryMode="PERSISTENT"/>
        <con:parameters/>
        <con:environmentSpec>
          <con:entry environmentId="9ba133c3-3cd7-45c8-8561-64ce1c92ea67">
            <con:authProfile>Inherit From Parent</con:authProfile>
          </con:entry>
          <con:entry environmentId="015736b2-7549-4743-94e6-d64278e45b46">
            <con:authProfile>Inherit From Parent</con:authProfile>
          </con:entry>
          <con:entry environmentId="e94bb75c-7f53-42a7-9f97-eb5aa3fcaf53">
            <con:authProfile>Inherit From Parent</con:authProfile>
          </con:entry>
          <con:entry environmentId="b2abf588-b45c-4e44-9dd0-8b2249bbc4e7">
            <con:authProfile>Inherit From Parent</con:authProfile>
          </con:entry>
          <con:entry environmentId="5322014d-43c7-4904-af96-4329a0290c25">
            <con:authProfile>Inherit From Parent</con:authProfile>
          </con:entry>
          <con:entry environmentId="495bcf82-3dfa-4956-b162-f0b1bfb07f10">
            <con:authProfile>Inherit From Parent</con:authProfile>
          </con:entry>
          <con:entry environmentId="feba3ebd-aeec-4c25-8b24-4b9e406c22cb">
            <con:authProfile>Inherit From Parent</con:authProfile>
          </con:entry>
          <con:entry environmentId="b3e5490d-8b80-49fe-97a2-a82077345345">
            <con:authProfile>Inherit From Parent</con:authProfile>
          </con:entry>
          <con:entry environmentId="a98aa22f-37cb-4a0e-84c6-f2de81903489">
            <con:authProfile>Inherit From Parent</con:authProfile>
          </con:entry>
        </con:environmentSpec>
      </con:restRequest>
    </con:config>
  </con:testStep>
  <con:testStep type="groovy" name="compareResponse" id="f1318de8-9be4-4070-bb6b-742ce90555e3">
    <con:settings/>
    <con:config>
      <script><![CDATA[//reference: https://support.smartbear.com/readyapi/faq/how-do-i-compare-two-dynamic-json-responses/
//compare before and after migration response
import groovy.json.*
import com.eviware.soapui.support.JsonPathValueNode
import com.fasterxml.jackson.databind.JsonNode
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.databind.node.ArrayNode
import com.fasterxml.jackson.databind.node.ObjectNode
import com.jayway.jsonpath.Configuration
import com.jayway.jsonpath.JsonPath
import com.jayway.jsonpath.spi.json.JacksonJsonNodeJsonProvider
import com.jayway.jsonpath.spi.mapper.JacksonMappingProvider
import com.eviware.soapui.model.testsuite.*;

def responseIsSame = true  //flag for validation

//get user's device OS and set directory
String osType = System.properties['os.name'][0..6];
def directory = "";

if (osType == "Windows"){
	directory = "C:/VPS_JSON/vpsResponseBeforeMigration/"
	}
else{
	directory = "/Applications/VPS_JSON/vpsResponseBeforeMigration/" //directory for mac
	}



//baseline response
def filePath = directory + "chargesAndFees-Royal.json"
def baselineResp = new JsonSlurper().parseText(new File(filePath).text)
baselineResp.status.remove("dateTime")
def modifiedBaselineResp = JsonOutput.toJson(baselineResp)

//current response
def currentJsonResp = context.expand('${chargesAndFees|Royal-After#Response}') 
def outputOfCurrentResp = new JsonSlurper().parseText(currentJsonResp)
outputOfCurrentResp.status.remove("dateTime")
def modifiedOutputOfCurrentResp = JsonOutput.toJson(outputOfCurrentResp)

log.info("Baseline Response: " + modifiedBaselineResp)
log.info("Output of CurrentResponse: " + modifiedOutputOfCurrentResp)

mapper = new ObjectMapper()
JsonNode responseBefore = mapper.readTree(modifiedBaselineResp);
JsonNode responseAfter = mapper.readTree(modifiedOutputOfCurrentResp);

Set<JsonPathValueNode> pathValues = listLeafValues(responseBefore, '$');
Set<JsonPathValueNode> pathValues2 = listLeafValues(responseAfter, '$');
Configuration configuration = Configuration.builder()
        .jsonProvider(new JacksonJsonNodeJsonProvider())
        .mappingProvider(new JacksonMappingProvider(mapper))
        .build();

pathValues.each {
    try {
        JsonPath jsonPath = JsonPath.compile(it.path);
        JsonNode result = jsonPath.read(responseAfter, configuration);
        
        if (result != it.valueNode) {
            log.info('Node with path ' + it.path + ' and value ' + it.valueNode + ' are not matched')
            responseIsSame = false
        }
    } catch (Exception e) {
        log.info('Node with path ' + it.path + ' and value ' + it.valueNode + ' are not matched')
        responseIsSame = false
    }
}

pathValues2.each {
    try {
        JsonPath jsonPath = JsonPath.compile(it.path);
        JsonNode result = jsonPath.read(responseBefore, configuration);
        
        if (result != it.valueNode) {
            log.info('Node with path ' + it.path + ' and value ' + it.valueNode + ' are not matched')
            responseIsSame = false
        }
    } catch (Exception e) {
        log.info('Node with path ' + it.path + ' and value ' + it.valueNode + ' are not matched')
        responseIsSame = false
    }
}

Set<JsonPathValueNode> listLeafValues(JsonNode jsonObject, String path) {
    if (jsonObject.isObject()) {
        return getValuesFrom((ObjectNode) jsonObject, path);
    } else if (jsonObject.isArray()) {
        return getValuesFrom((ArrayNode) jsonObject, path);
    } else {
    }
}

Set<JsonPathValueNode> getValuesFrom(ObjectNode jsonObject, String path) {
    Set<JsonPathValueNode> values = new LinkedHashSet<>();
    Iterator<Map.Entry<String, JsonNode>> fields = jsonObject.fields();
    while (fields.hasNext()) {
        Map.Entry<String, JsonNode> field = fields.next();
        JsonNode value = field.getValue();
        String nodePath = path + "." + field.getKey();
        addValue(values, value, nodePath);
    }
    return values;
}

Set<JsonPathValueNode> getValuesFrom(ArrayNode array, String nodePath) {
    Set<JsonPathValueNode> values = new LinkedHashSet<>();
    int index = 0;
    for (JsonNode value : array) {
        addValue(values, value, nodePath + "[" + index + "]");
        index++;
    }
    return values;
}

def addValue(Set<JsonPathValueNode> values, JsonNode value, String nodePath) {
    if (value.isObject()) {
        values.addAll(getValuesFrom((ObjectNode) value, nodePath));
    } else if (value.isArray()) {
        values.addAll(getValuesFrom((ArrayNode) value, nodePath));
    } else if (value.isValueNode()) {
        values.add(new JsonPathValueNode(nodePath, value));
    }
}

assert responseIsSame == true
log.info("Before Migration and After Migration Responses are matched")]]></script>
    </con:config>
  </con:testStep>
  <con:properties>
    <con:property>
      <con:name>shipCode</con:name>
      <con:value>NV</con:value>
    </con:property>
    <con:property>
      <con:name>sailDate</con:name>
      <con:value>20250103</con:value>
    </con:property>
    <con:property>
      <con:name>currencyCode</con:name>
      <con:value>USD</con:value>
    </con:property>
    <con:property>
      <con:name>itineraryCode</con:name>
      <con:value>03X023</con:value>
    </con:property>
    <con:property>
      <con:name>currentDate</con:name>
      <con:value>20240205</con:value>
    </con:property>
  </con:properties>
  <con:reportParameters/>
</con:testCase>
